import React from 'react';

const StatusBar = ({ progress, current }) => (
  <div className="status-bar">
    <hr className="status-line-thick" />
    <hr className="status-line" />
  </div>
);

export default StatusBar;
